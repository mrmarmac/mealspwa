name = "meals-recipe-fetcher"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Stateless — no KV namespaces, no Durable Objects, no D1. Nothing here
# should ever need to persist anything; if a future change needs storage,
# that's a sign it no longer belongs in this worker.
